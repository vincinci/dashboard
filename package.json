{
  "name": "iwanyu-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 